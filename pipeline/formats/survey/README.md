# Inbox survey artifacts

Generated by `python pipeline/survey_inbox.py --days N`.

Columns use the attribution triad: `source` (domain), `sub_source` (email),
`nickname` (label), plus `format_family` (internal splitter key).

| File | Contents |
|------|----------|
| `inbox_5d.json` | Full inventory: sender, subject, open lines, route, yield, signals |
| `inbox_5d.csv` | Flat summary |
| `bodies/*.txt` | Full stripped bodies (local analysis; do not commit secrets) |
| `structure_notes.txt` | One-off structure dump from `analyze_survey.py` |

**Baseline survey:** 2026-08-02 · 61 msgs · `newer_than:5d deliveredto:dirk@tullyinvesting.com in:anywhere`

Repertoire rebuilt from this inventory — see `../repertoire.yaml` and
`docs/deal-format-repertoire.md`.
