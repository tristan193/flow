# Place secrets here (gitignored):
#   client_secret.json  — downloaded from Google Cloud Console (Desktop client)
#   token.json          — written by `python gmail_auth.py`
#   apify_token.txt     — Apify API token (one line) for BizBuySell enrich
